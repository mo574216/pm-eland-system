# Infrastructure

This directory is reserved for container, local-development, deployment, observability, and operations artifacts.

Planned foundation work:

- `FND-005`: local Docker Compose for frontend, backend, PostgreSQL, MinIO, and Redis.
- `FND-006`: CI baseline.
- `M6`: production deployment, observability, backup, and restore procedures.

Current status: not initialized.
