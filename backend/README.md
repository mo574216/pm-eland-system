# Backend

This directory is reserved for the FastAPI application defined by `FND-002` and the PostgreSQL/Alembic foundation defined by `FND-004`.

Backend code must follow the layered, domain-agnostic architecture in `ai_context/05_BACKEND_SPECIFICATION.md`. No configurable business concept may receive a dedicated model, table, service, or API.

Current status: not initialized.
